<div align="center">

# Gimi

**一个真正住在你手机里的 AI 助手。**

可以聊天，可以说话，也可以真的替你做事 —— 定闹钟、找文件、控制音乐、查看日程，
全都不用离开这个 App。

[English](README.md) · [简体中文](README.zh-CN.md)

</div>

---

## Gimi 是什么？

大多数 AI 应用，本质上是一个输入框加一个模型。Gimi 是一个输入框，
背后连着的是**你的手机**。

你填入自己已有的模型 API Key，Gimi 就把这个模型变成一个能真正操作设备的助手：
闹钟、日程、媒体播放、屏幕亮度、你授权的文件夹，以及任何你自己接入的外部工具。

不绑定任何一家厂商，不需要订阅，助手想做的每一步操作，你都可以先看一眼再决定。

## 主要能力

### 不只是聊天的聊天

回复实时流式输出，可以从相机或相册添加图片，侧边抽屉里随时翻回历史会话，
回复还能朗读出来。聊到一半也能随时切换模型。最多可以同时跑 3 个任务，
每个任务的进度一目了然。

### 解放双手的语音

点一下麦克风就能语音输入；也可以设置**唤醒词**让 Gimi 在后台待命 ——
配合蓝牙耳机，你可以完全不碰手机就把事情交代出去。唤醒词识别全程在本机完成，
中文模型已内置在安装包里，英文模型是一个可选的小体积下载。

### 真正能操作设备的工具

助手内置了一整套工具，需要的时候会自己拿来用：

| | |
|---|---|
| **时间** | 定闹钟、设倒计时、查看当前时间 |
| **日历** | 查看近期日程、创建日程 |
| **媒体** | 播放、暂停、切歌 —— 包括其他 App 里正在放的内容 |
| **音量** | 读取和调节媒体音量 |
| **显示** | 屏幕亮度、自动亮度、自动息屏时间 |
| **位置** | 获取当前位置、在地图中打开 |
| **文件** | 搜索图片、视频、音频，以及你授权的文档 |
| **应用** | 查看、搜索、打开已安装应用；拍照或录像 |
| **联系** | 拨号、起草短信、查找联系人 |
| **网页** | 联网搜索、打开链接 |
| **设置** | 直接跳转到对应的系统设置页 |

### 用 MCP 接入你自己的工具

连接远程 **MCP 服务器**（支持 SSE 与 Streamable HTTP），给助手补上 Gimi
本身没有的能力。可以手动添加，也可以直接粘贴现成的 `mcpServers` JSON
配置批量导入。每个服务器都能随时开关。

### 技能（Skills）

从链接或本地 ZIP 安装可复用的技能包。一个技能就是一份指引和配套资源的合集，
装一次，之后在需要的场景里助手就会用上。

### 工作文件

挑选你希望助手能够检索的文件夹。它只能看到你明确授权的目录，
授权也可以随时收回。

### 掌控权始终在你手里

- **先问再做** —— 敏感操作会先暂停，等你点「允许」或「拒绝」。
- **逐个授权工具** —— 可以把工具箱精简到只剩你愿意开放的那几个。
- **说人话的权限页** —— 一个页面讲清楚每项系统权限用来做什么，随时授予或撤销。
- **不多传一个字节** —— API Key 只保存在本机，直接和你选的服务商通信。

## 支持的模型服务

以下服务都可以填入自己的 API Key：

<div align="center">

| | | | |
|---|---|---|---|
| **OpenAI** | **Anthropic** | **DeepSeek** | **月之暗面 Kimi** |
| **智谱 GLM** | **MiniMax** | **小米 MiMo** | |

</div>

同时支持 OpenAI 兼容协议和 Anthropic 协议，所以大部分其他服务也能用 ——
填一个自定义 API 地址即可。

除了对话模型，你还可以分别配置：

- **快速模型**：自动为会话生成标题；
- **语音识别模型**：用于语音输入；
- **语音合成模型与音色**：用于朗读回复。

## 快速上手

1. **安装应用。** 下载 Release 里的 APK。Gimi 需要 Android 15 及以上版本。
2. **添加模型服务。** 打开 *设置 → 模型服务*，选择服务商，粘贴 API Key，
   点「检测」确认可用。
3. **设置默认模型。** 在 *设置 → 默认模型* 里选好助手使用的模型；
   想用语音的话，语音模型也在这里配置。
4. **授予权限。** *设置 → 权限* 会逐条说明需要什么、为什么需要。
   全部都是可选的，不想给的直接跳过。
5. **开始聊天。** 就这样。

之后想扩展的时候：开启**语音唤醒**解放双手、添加 **MCP 服务器**扩充工具、
安装**技能**、或者授权一个**工作文件夹**。

## 技术构成

<details>
<summary>它是怎么搭起来的</summary>

Gimi 是一个纯 Kotlin 编写的多模块 Android 应用，UI 使用 Jetpack Compose 与
Material 3。每个能力 —— 聊天、语音、模型、工具、技能 —— 各自拥有独立的
`domain` / `data` / `feature` 模块，功能之间互不依赖。

Agent 编排基于 **ADK Kotlin**，聊天中的 Markdown 由
**multiplatform-markdown-renderer** 渲染，唤醒词识别使用 **Vosk**，
工具检索基于本机句向量模型，外部工具则通过 **Model Context Protocol** 接入。
完整的开发约定见 [`AGENTS.md`](AGENTS.md)。

</details>

## 致谢

Gimi 站在了很多优秀开源项目的肩膀上：

- **[GetStream/chat-ai-samples](https://github.com/GetStream/chat-ai-samples)**

- **[mikepenz/multiplatform-markdown-renderer](https://github.com/mikepenz/multiplatform-markdown-renderer)**

- **[google/adk-kotlin](https://github.com/google/adk-kotlin)**

## 开源协议

Gimi 基于 [Apache License 2.0](LICENSE) 开源。
